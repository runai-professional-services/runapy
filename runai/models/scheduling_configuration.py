# coding: utf-8

"""
NVIDIA Run:ai

# Introduction  The NVIDIA Run:ai Control-Plane API reference is a guide that provides an easy-to-use programming interface for adding various tasks to your application, including workload submission, resource management, and administrative operations.  NVIDIA Run:ai APIs are accessed using *bearer tokens*. To obtain a token, you need to create a **Service account** through the NVIDIA Run:ai user interface. To create a service account, in your UI, go to Access → Service Accounts (for organization-level service accounts) or User settings → Access Keys (for user access keys), and create a new one.  After you have created a new service account, you will need to assign it access rules. To assign access rules to the service account, see [Create access rules](https://run-ai-docs.nvidia.com/saas/infrastructure-setup/authentication/accessrules#create-or-delete-rules). Make sure you assign the correct rules to your service account. Use the [Roles](https://run-ai-docs.nvidia.com/saas/infrastructure-setup/authentication/roles) to assign the correct access rules.  To get your access token, follow the instructions in [Request a token](https://run-ai-docs.nvidia.com/saas/reference/api/rest-auth/#request-an-api-token).

The version of the OpenAPI document: latest
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from runai.models.scheduling_configuration_placement_strategy import (
    SchedulingConfigurationPlacementStrategy,
)
from typing import Optional, Set
from typing_extensions import Self


class SchedulingConfiguration(BaseModel):
    """
    Pydantic class model representing SchedulingConfiguration.

    Parameters:
        ```python
        placement_strategy: SchedulingConfigurationPlacementStrategy
        min_guaranteed_runtime: Optional[str]
        ```
        placement_strategy: See model SchedulingConfigurationPlacementStrategy for more information.
        min_guaranteed_runtime: Minimum duration (e.g., 5d8h40m) that a preemptible workload is guaranteed to run once scheduled and bound to a node. During this period, it is considered non-preemptible even if a higher-priority workload becomes eligible. Defaults to 0 (immediate preemption allowed).
    Example:
        ```python
        SchedulingConfiguration(
            placement_strategy=runai.models.scheduling_configuration_placement_strategy.SchedulingConfiguration_placementStrategy(
                    cpu = 'spread',
                    gpu = 'spread', ),
                        min_guaranteed_runtime='5d8h40m'
        )
        ```
    """  # noqa: E501

    placement_strategy: Optional[SchedulingConfigurationPlacementStrategy] = Field(
        default=None, alias="placementStrategy"
    )
    min_guaranteed_runtime: Optional[Annotated[str, Field(strict=True)]] = Field(
        default=None,
        description="Minimum duration (e.g., 5d8h40m) that a preemptible workload is guaranteed to run once scheduled and bound to a node. During this period, it is considered non-preemptible even if a higher-priority workload becomes eligible. Defaults to 0 (immediate preemption allowed).",
        alias="minGuaranteedRuntime",
    )
    __properties: ClassVar[List[str]] = ["placementStrategy", "minGuaranteedRuntime"]

    @field_validator("min_guaranteed_runtime")
    def min_guaranteed_runtime_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^(0|[+]?((\d+(\.\d+)?)(ns|us|µs|μs|ms|s|m|h|d|w))+$)", value):
            raise ValueError(
                r"must validate the regular expression /^(0|[+]?((\d+(\.\d+)?)(ns|us|µs|μs|ms|s|m|h|d|w))+$)/"
            )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SchedulingConfiguration from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of placement_strategy
        if self.placement_strategy:
            _dict["placementStrategy"] = self.placement_strategy.to_dict()
        # set to None if min_guaranteed_runtime (nullable) is None
        # and model_fields_set contains the field
        if (
            self.min_guaranteed_runtime is None
            and "min_guaranteed_runtime" in self.model_fields_set
        ):
            _dict["minGuaranteedRuntime"] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SchedulingConfiguration from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "placementStrategy": (
                    SchedulingConfigurationPlacementStrategy.from_dict(
                        obj["placementStrategy"]
                    )
                    if obj.get("placementStrategy") is not None
                    else None
                ),
                "minGuaranteedRuntime": obj.get("minGuaranteedRuntime"),
            }
        )
        return _obj
