# coding: utf-8

"""
NVIDIA Run:ai

# Introduction  The NVIDIA Run:ai Control-Plane API reference is a guide that provides an easy-to-use programming interface for adding various tasks to your application, including workload submission, resource management, and administrative operations.  NVIDIA Run:ai APIs are accessed using *bearer tokens*. To obtain a token, you need to create a **Service account** through the NVIDIA Run:ai user interface. To create a service account, in your UI, go to Access → Service Accounts (for organization-level service accounts) or User settings → Access Keys (for user access keys), and create a new one.  After you have created a new service account, you will need to assign it access rules. To assign access rules to the service account, see [Create access rules](https://run-ai-docs.nvidia.com/saas/infrastructure-setup/authentication/accessrules#create-or-delete-rules). Make sure you assign the correct rules to your service account. Use the [Roles](https://run-ai-docs.nvidia.com/saas/infrastructure-setup/authentication/roles) to assign the correct access rules.  To get your access token, follow the instructions in [Request a token](https://run-ai-docs.nvidia.com/saas/reference/api/rest-auth/#request-an-api-token).

The version of the OpenAPI document: latest
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self


class NimServiceDefaultsAllOfAutoscaling(BaseModel):
    """
    Pydantic class model representing NimServiceDefaultsAllOfAutoscaling.

    Parameters:
        ```python
        max_replicas: Optional[int]
        metric: Optional[str]
        metric_threshold: Optional[int]
        min_replicas: Optional[int]
        scale_window_seconds: Optional[int]
        ```
        max_replicas: The maximum number of replicas for autoscaling. Defaults to minReplicas. Must be no less than minReplicas.
        metric: The metric to use for autoscaling (mandatory).
        metric_threshold: The threshold to use with the specified metric for autoscaling (mandatory).
        min_replicas: The minimum number of replicas for autoscaling. Defaults to 1.
        scale_window_seconds: The time window for autoscaling decisions, in seconds. Defaults to 300 seconds.
    Example:
        ```python
        NimServiceDefaultsAllOfAutoscaling(
            max_replicas=1,
                        metric='http_requests_total',
                        metric_threshold=56,
                        min_replicas=1,
                        scale_window_seconds=60
        )
        ```
    """  # noqa: E501

    max_replicas: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(
        default=None,
        description="The maximum number of replicas for autoscaling. Defaults to minReplicas. Must be no less than minReplicas.",
        alias="maxReplicas",
    )
    metric: Optional[Annotated[str, Field(strict=True)]] = Field(
        default=None, description="The metric to use for autoscaling (mandatory)."
    )
    metric_threshold: Optional[StrictInt] = Field(
        default=None,
        description="The threshold to use with the specified metric for autoscaling (mandatory).",
        alias="metricThreshold",
    )
    min_replicas: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(
        default=None,
        description="The minimum number of replicas for autoscaling. Defaults to 1.",
        alias="minReplicas",
    )
    scale_window_seconds: Optional[
        Annotated[int, Field(le=3600, strict=True, ge=60)]
    ] = Field(
        default=None,
        description="The time window for autoscaling decisions, in seconds. Defaults to 300 seconds.",
        alias="scaleWindowSeconds",
    )
    __properties: ClassVar[List[str]] = [
        "maxReplicas",
        "metric",
        "metricThreshold",
        "minReplicas",
        "scaleWindowSeconds",
    ]

    @field_validator("metric")
    def metric_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z_:][a-zA-Z0-9_:]*$", value):
            raise ValueError(
                r"must validate the regular expression /^[a-zA-Z_:][a-zA-Z0-9_:]*$/"
            )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NimServiceDefaultsAllOfAutoscaling from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if max_replicas (nullable) is None
        # and model_fields_set contains the field
        if self.max_replicas is None and "max_replicas" in self.model_fields_set:
            _dict["maxReplicas"] = None

        # set to None if metric (nullable) is None
        # and model_fields_set contains the field
        if self.metric is None and "metric" in self.model_fields_set:
            _dict["metric"] = None

        # set to None if metric_threshold (nullable) is None
        # and model_fields_set contains the field
        if (
            self.metric_threshold is None
            and "metric_threshold" in self.model_fields_set
        ):
            _dict["metricThreshold"] = None

        # set to None if min_replicas (nullable) is None
        # and model_fields_set contains the field
        if self.min_replicas is None and "min_replicas" in self.model_fields_set:
            _dict["minReplicas"] = None

        # set to None if scale_window_seconds (nullable) is None
        # and model_fields_set contains the field
        if (
            self.scale_window_seconds is None
            and "scale_window_seconds" in self.model_fields_set
        ):
            _dict["scaleWindowSeconds"] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NimServiceDefaultsAllOfAutoscaling from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "maxReplicas": obj.get("maxReplicas"),
                "metric": obj.get("metric"),
                "metricThreshold": obj.get("metricThreshold"),
                "minReplicas": obj.get("minReplicas"),
                "scaleWindowSeconds": obj.get("scaleWindowSeconds"),
            }
        )
        return _obj
