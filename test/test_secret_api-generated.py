# coding: utf-8

"""
Test file for SecretApi
Generated by OpenAPI Generator with custom template
"""

import pytest
import unittest.mock as mock
from datetime import datetime, timezone
import json

from runai.configuration import Configuration
from runai.api_client import ApiClient
from runai.models import *
from runai.exceptions import ApiException


class TestSecretApi:
    """Test cases for SecretApi"""

    @pytest.fixture(autouse=True)
    def setup(self):
        """Setup test fixtures"""
        self.configuration = Configuration(
            client_id="test-client",
            client_secret="test-secret",
            runai_base_url="https://test.run.ai",
        )
        self.api_client = ApiClient(self.configuration)
        self.api = SecretApi(self.api_client)

        # Mock the request method
        self.request_patcher = mock.patch.object(self.api_client.rest_client, "request")
        self.mock_request = self.request_patcher.start()
        yield
        self.request_patcher.stop()

    def test_create_secret_asset(self):
        """Test case for create_secret_asset

        Create a Secret datasource asset. Use to create a Secret datasource asset.
        """
        # Mock response
        mock_response = mock.Mock()
        mock_response.status = 200
        mock_response.read.return_value = json.dumps({"data": {}})
        self.mock_request.return_value = mock_response

        # Test parameters
        secret_asset_creation_request = (
            runai.SecretAssetCreationRequest()
        )  # SecretAssetCreationRequest |

        # Make request
        response = self.api.create_secret_asset()

        # Verify request was made
        assert self.mock_request.called
        args, kwargs = self.mock_request.call_args

        # Verify request method and URL
        assert kwargs["method"] == "POST"
        assert "/api/v1/asset/datasource/secrets" in kwargs["url"]

        # Verify body
        assert kwargs["body"] is not None

        # Verify response
        assert isinstance(response, SecretAsset)

    def test_create_secret_asset_error(self):
        """Test error handling for create_secret_asset"""
        # Mock error response
        mock_response = mock.Mock()
        mock_response.status = 400
        mock_response.read.return_value = json.dumps({"message": "Error message"})
        self.mock_request.return_value = mock_response

        # Test parameters

        # Verify error handling
        with pytest.raises(ApiException) as exc_info:
            self.api.create_secret_asset()
        assert exc_info.value.status == 400

    def test_delete_secret_asset_by_id(self):
        """Test case for delete_secret_asset_by_id

        Delete a Secret asset. Use to delete a Secret datasource asset by id.
        """
        # Mock response
        mock_response = mock.Mock()
        mock_response.status = 200
        mock_response.read.return_value = json.dumps({"data": {}})
        self.mock_request.return_value = mock_response

        # Test parameters
        asset_id = "asset_id_example"  # str | Unique identifier of the asset.

        # Make request
        response = self.api.delete_secret_asset_by_id(
            asset_id=asset_id,
        )

        # Verify request was made
        assert self.mock_request.called
        args, kwargs = self.mock_request.call_args

        # Verify request method and URL
        assert kwargs["method"] == "DELETE"
        assert "/api/v1/asset/datasource/secrets/{AssetId}" in kwargs["url"]

        # Verify response
        assert isinstance(response, HttpResponse)

    def test_delete_secret_asset_by_id_error(self):
        """Test error handling for delete_secret_asset_by_id"""
        # Mock error response
        mock_response = mock.Mock()
        mock_response.status = 400
        mock_response.read.return_value = json.dumps({"message": "Error message"})
        self.mock_request.return_value = mock_response

        # Test parameters
        asset_id = "asset_id_example"

        # Verify error handling
        with pytest.raises(ApiException) as exc_info:
            self.api.delete_secret_asset_by_id(
                asset_id=asset_id,
            )
        assert exc_info.value.status == 400

    def test_get_secret_asset_by_id(self):
        """Test case for get_secret_asset_by_id

        Get a Secret asset. Retrieve the details of Secret datasource asset by id.
        """
        # Mock response
        mock_response = mock.Mock()
        mock_response.status = 200
        mock_response.read.return_value = json.dumps({"data": {}})
        self.mock_request.return_value = mock_response

        # Test parameters
        asset_id = "asset_id_example"  # str | Unique identifier of the asset.
        usage_info = True  # bool | Whether the query should include asset usage information as part of the response.
        comply_to_project = 56  # int | Include workload creation compliance information of an asset, for a given project, as part of the response. To check compliance, you need to provide both project id and workload type.
        comply_to_workload_type = "comply_to_workload_type_example"  # str | Include workload creation compliance information of an asset, for a given workload type, as part of the response. To check compliance, you need to provide both project id and workload type.
        status_info = True  # bool | Whether the query should include asset status information as part of the response.
        comply_to_replica_type = "comply_to_replica_type_example"  # str | Include workload creation compliance information of an asset, for a given replica type, as part of the response. To check compliance, you need to provide both project id and workload type. For distributed, replica type should be provided as well.

        # Make request
        response = self.api.get_secret_asset_by_id(
            asset_id=asset_id,
        )

        # Verify request was made
        assert self.mock_request.called
        args, kwargs = self.mock_request.call_args

        # Verify request method and URL
        assert kwargs["method"] == "GET"
        assert "/api/v1/asset/datasource/secrets/{AssetId}" in kwargs["url"]

        # Verify query parameters
        assert "usageInfo=" in kwargs["url"]
        # Verify query parameters
        assert "complyToProject=" in kwargs["url"]
        # Verify query parameters
        assert "complyToWorkloadType=" in kwargs["url"]
        # Verify query parameters
        assert "statusInfo=" in kwargs["url"]
        # Verify query parameters
        assert "complyToReplicaType=" in kwargs["url"]

        # Verify response
        assert isinstance(response, SecretAsset)

    def test_get_secret_asset_by_id_error(self):
        """Test error handling for get_secret_asset_by_id"""
        # Mock error response
        mock_response = mock.Mock()
        mock_response.status = 400
        mock_response.read.return_value = json.dumps({"message": "Error message"})
        self.mock_request.return_value = mock_response

        # Test parameters
        asset_id = "asset_id_example"

        # Verify error handling
        with pytest.raises(ApiException) as exc_info:
            self.api.get_secret_asset_by_id(
                asset_id=asset_id,
            )
        assert exc_info.value.status == 400

    def test_list_secret_assets(self):
        """Test case for list_secret_assets

        List Secret datasource assets. Retrieve a list of Secret datasource assets.
        """
        # Mock response
        mock_response = mock.Mock()
        mock_response.status = 200
        mock_response.read.return_value = json.dumps({"data": {}})
        self.mock_request.return_value = mock_response

        # Test parameters
        name = "name_example"  # str | Filter results by name.
        scope = "scope_example"  # str | Filter results by scope.
        project_id = 56  # int | Filter results by project id. If scope filter is project, only assets from the specific project will be included in the response. Otherwise, the response will include project, department, cluster, tenant and system assets.
        department_id = "1"  # str | Filter results by department id. If scope filter is department, only assets from the specific department will be included in the response. Otherwise, the response will include department, cluster, tenant and system assets.
        cluster_id = "d73a738f-fab3-430a-8fa3-5241493d7128"  # str | Filter results by Universally Unique Identifier (UUID) of the cluster. If scope filter is cluster, only assets from the specific cluster will be included in the response. Otherwise, the response will include cluster, tenant and system assets.
        usage_info = True  # bool | Whether the query should include asset usage information as part of the response.
        comply_to_project = 56  # int | Include workload creation compliance information of an asset, for a given project, as part of the response. To check compliance, you need to provide both project id and workload type.
        comply_to_workload_type = "comply_to_workload_type_example"  # str | Include workload creation compliance information of an asset, for a given workload type, as part of the response. To check compliance, you need to provide both project id and workload type.
        status_info = True  # bool | Whether the query should include asset status information as part of the response.
        asset_ids = "dbf4767e-2fa1-43b0-97a2-7c0cecda180b,550e8400-e29b-41d4-a716-44665544000a"  # str | Filter results by the ids of the assets. Provided value should be a comma separated string of UUIDs.
        comply_to_replica_type = "comply_to_replica_type_example"  # str | Include workload creation compliance information of an asset, for a given replica type, as part of the response. To check compliance, you need to provide both project id and workload type. For distributed, replica type should be provided as well.

        # Make request
        response = self.api.list_secret_assets()

        # Verify request was made
        assert self.mock_request.called
        args, kwargs = self.mock_request.call_args

        # Verify request method and URL
        assert kwargs["method"] == "GET"
        assert "/api/v1/asset/datasource/secrets" in kwargs["url"]

        # Verify query parameters
        assert "name=" in kwargs["url"]
        # Verify query parameters
        assert "scope=" in kwargs["url"]
        # Verify query parameters
        assert "projectId=" in kwargs["url"]
        # Verify query parameters
        assert "departmentId=" in kwargs["url"]
        # Verify query parameters
        assert "clusterId=" in kwargs["url"]
        # Verify query parameters
        assert "usageInfo=" in kwargs["url"]
        # Verify query parameters
        assert "complyToProject=" in kwargs["url"]
        # Verify query parameters
        assert "complyToWorkloadType=" in kwargs["url"]
        # Verify query parameters
        assert "statusInfo=" in kwargs["url"]
        # Verify query parameters
        assert "assetIds=" in kwargs["url"]
        # Verify query parameters
        assert "complyToReplicaType=" in kwargs["url"]

        # Verify response
        assert isinstance(response, SecretAssetListResponse)

    def test_list_secret_assets_error(self):
        """Test error handling for list_secret_assets"""
        # Mock error response
        mock_response = mock.Mock()
        mock_response.status = 400
        mock_response.read.return_value = json.dumps({"message": "Error message"})
        self.mock_request.return_value = mock_response

        # Test parameters

        # Verify error handling
        with pytest.raises(ApiException) as exc_info:
            self.api.list_secret_assets()
        assert exc_info.value.status == 400

    def test_update_secret_asset_by_id(self):
        """Test case for update_secret_asset_by_id

        Update a Secret asset. Use to update the details of a Secret datasource asset by id.
        """
        # Mock response
        mock_response = mock.Mock()
        mock_response.status = 200
        mock_response.read.return_value = json.dumps({"data": {}})
        self.mock_request.return_value = mock_response

        # Test parameters
        asset_id = "asset_id_example"  # str | Unique identifier of the asset.
        secret_asset_update_request = (
            runai.SecretAssetUpdateRequest()
        )  # SecretAssetUpdateRequest |

        # Make request
        response = self.api.update_secret_asset_by_id(
            asset_id=asset_id,
        )

        # Verify request was made
        assert self.mock_request.called
        args, kwargs = self.mock_request.call_args

        # Verify request method and URL
        assert kwargs["method"] == "PUT"
        assert "/api/v1/asset/datasource/secrets/{AssetId}" in kwargs["url"]

        # Verify body
        assert kwargs["body"] is not None

        # Verify response
        assert isinstance(response, SecretAsset)

    def test_update_secret_asset_by_id_error(self):
        """Test error handling for update_secret_asset_by_id"""
        # Mock error response
        mock_response = mock.Mock()
        mock_response.status = 400
        mock_response.read.return_value = json.dumps({"message": "Error message"})
        self.mock_request.return_value = mock_response

        # Test parameters
        asset_id = "asset_id_example"

        # Verify error handling
        with pytest.raises(ApiException) as exc_info:
            self.api.update_secret_asset_by_id(
                asset_id=asset_id,
            )
        assert exc_info.value.status == 400
